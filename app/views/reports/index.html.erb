<div class="container">
  <div class="form-inline">
    <%= search_form_for @q do |f| %>
      <%= f.search_field :document_title_cont, placeholder: 'title', class: "search-input" %>
      <%= f.submit class: "btn-reserved" %>
    <% end %>
    <div class="ml-2" style="padding-top: 1rem;">
      <%= link_to users_path, class: "btn btn-sm btn-inverse" do %>
        <i class="fa fa-sync"></i>
      <% end %>
    </div>
  </div>

  <div class="table responsive">
  <table class="table table-bordered table-striped table-sm">
    <thead>
      <tr>
        <th>Document Title</th>
        <th>Originator</th>
        <th>Date</th>
      </tr>
    </thead>
    <tbody>
      <% @reports.each do |report| %>
        <tr>
          <td><%= report.document_title %></td>
          <td><%= report.author %></td>
          <td><%= report.date %></td>
          <div>
            <%= link_to 'Show', report %>
            <%= link_to 'Edit', report_report_wizard_index_path(report) %>
            <%= link_to 'Destroy', report, method: :delete, data: { confirm: 'Are you sure?' } %>
          </div>
      </tr>
        </tr>
      <% end %>
    </tbody>
  </table>
  </div>
</div>